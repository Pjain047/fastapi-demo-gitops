apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fastapi-demo.fullname" . }}-config
  namespace: {{ include "fastapi-demo.namespace" . }}
  labels:
    {{- include "fastapi-demo.labels" . | nindent 4 }}
  
data:
{{- toYaml .Values.config | nindent 2 }}
